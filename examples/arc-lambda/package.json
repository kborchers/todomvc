{
  "private": true,
  "scripts": {
    "create": "AWS_PROFILE=default AWS_REGION=us-east-1 arc-create",
    "deploy": "AWS_PROFILE=default AWS_REGION=us-east-1 arc-deploy",
    "start": "NODE_ENV=testing AWS_REGION=us-east-1 arc-sandbox",
    "i": "arc-modules-install",
    "link": "arc-modules-link",
    "uninstall": "arc-modules-uninstall",
    "update": "arc-modules-update"
  },
  "dependencies": {
    "@architect/workflows": "^1.0.19"
  },
  "devDependencies": {
    "jasmine-core": "^2.0.0"
  }
}
